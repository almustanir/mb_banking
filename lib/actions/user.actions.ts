'use server';

export const signIn = async () => {
    try {
        //Mutation / Database / Make Fetch Api
    } catch (error) {
        console.error('Error', error);
    }
}
export const signUp = async (userData: SignUpParams) => {
    try {
        //create a user acc
    } catch (error) {
        console.error('Error', error);
    }
}